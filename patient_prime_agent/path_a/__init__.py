"""Path A: the legacy schema-validated pipeline -- planner, orchestrator (harness), the
generic extractors/, and the skills/ instructions they read -- driving `python -m
patient_prime_agent` and `python -m patient_prime_agent --agentic`."""
